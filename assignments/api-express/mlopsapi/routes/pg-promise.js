// var express = require("express");
// var router = express.Router();
// const { UserHandler } = require("../handler/UserHandler");

// const userHandler = new UserHandler();

// router.get("/user", userHandler.readAll.bind(userHandler));
// router.get("/user/:id", userHandler.read.bind(userHandler));
// router.post("/user", userHandler.create.bind(userHandler));
// router.put("/user/:id", userHandler.update.bind(userHandler));
// router.delete("/user/:id", userHandler.delete.bind(userHandler));

// module.exports = router;
